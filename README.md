# SSH Fleet Setup

A containerized fleet of SSH-accessible nodes using Docker Compose and a Dockerfile-based setup for reliable container inits.

## Quick Start

```bash
just go
```

- this sets up designated keys, validates your compose and dockerfiles for unique port mappings, and runs them.
- see Justfile for further options
